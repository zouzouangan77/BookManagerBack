apply plugin: "war"

bootWar {
    mainClass = "com.samuelangan.mycompagny.BookmanagerbackApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
